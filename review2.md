# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
- <input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか
- ルート定義で指定したルートパラメータのIDで、選んだTodoのIDカラムの値が入っている。

### findメソッドで実行しているSQLは何か
- SELECT * FROM todos WHERE ID = {$ID};

### findメソッドで取得できる値は何か
- $idとIDカラムの値が一致するレコードのCollectionクラス。

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
- save()が処理される際に、指定されたIDと同じIDがデータベース上にあるかないかを基準に切り替えを行っている。

## Todo論理削除

### traitとclassの違いとは
- 継承と異なり、1つのクラスに複数のトレイトを追加することができる。
- トレイト自体はインスタンス化することができない。

### traitを使用するメリットとは
- 継承をしなくても機能を共有することができる。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
- TodoCOntrollerのクラスが呼び出されたタイミング、ルート関数でURLを指定してそのURLに対して処理を実行する時。

### RequestクラスからFormRequestクラスに変更した理由
- バリデーションを実行するため。

### $errorsのhasメソッドの引数・返り値は何か
- 引数はテキストボックスに入力した値のname属性'content'
- 返り値はエラーが発生しているかしていないかのbool型

### $errorsのfirstメソッドの引数・返り値は何か
- 引数はテキストボックスに入力した値のname属性'content'
- 引数の項目のうち、Messageの中で最初に発生したエラーのエラーメッセージ。

### フレームワークとは何か
- webアプリケーション開発を効率化するために汎用的な機能があらかじめ用意されている枠組みのこと。

### MVCはどういったアーキテクチャか
- webアプリケーションの持つ機能をModel,View,Controllerに大きく分けて開発を行う設計志向のこと。

### ORMとは何か、またLaravelが使用しているORMは何か
- オブジェクト指向プラグらミング言語とリレーショナルデータベース間のやり取りを取りまとめてくれる技術のこと。
- Eloquent ORM

### composer.json, composer.lockとは何か
- composer.jsonはインストールされたパッケージ一覧を表示するファイル
- composer.lockは実際にインストールされたパッケージの情報が記録されるファイル

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
- vendorディレクトリ

