# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
- select * from todo;

### Todoモデルのallメソッドの返り値は何か
- Collectionクラス

### 配列の代わりにCollectionクラスを使用するメリットは
- メソッドを利用して、データの操作、変換、フィルタリングを簡単行うことができる点。

### view関数の第1・第2引数の指定と何をしているか
- 第一引数は画面に表示したいtodo.indexのbladeファイルで第二引数は渡したいデータの['todos' => $todos]
- $todosはインスタンス化したTodoクラスにallメソッドを使ってtodosテーブルのレコードが取得されたCollectionクラスで、そのデータをバリュー、'todos'のキーとした連想配列でtodo.indexのBladeファイルに渡している。

### index.blade.phpの$todos・$todoに代入されているものは何か
- $todosはTodoController.phpから渡されたtodosテーブルのすべてのレコードを持ったCollectionクラスが代入されている。
- $todoは$todosの配列のバリューである一つ一つのレコードが入った配列。

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
- POST形式で送信されたHTTPリクエストのすべてのデータを連想配列の形で取得している。

### fillメソッドは何をしているか
- 取得した連想配列をTodoインスタンスの各プロパティに一括で代入

### $fillableは何のために設定しているか
- モデルに代入するプロパティを指定することでフォームを改ざんして不正な操作を防ぐために設定している。

### saveメソッドで実行しているSQLは何か
- insert into todos ('content') values (入力された値)

### redirect()->route()は何をしているか
- route()で指定したBladeファイルを指定してそのページにリダイレクトしている。

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
- sqlを記入しなくてもphpコードでテーブル操作を行える。
- マイグレーションファイルをgitで共有することで複数の開発者が同じテーブル構成で作業することができる。

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
- artisanコマンド　
  (up) php artisan migrate
  (down) php artisan migrate:rollback

### Seederクラスの役割は何か
- sqlを書かずにphpコードでテストデータを投入できる。
- シーダーファイルをgit管理することでテストデータを開発者間で共有できる。

### route関数の引数・返り値・使用するメリット
- 第一引数にURIで第二引数にそのURIとHTTPメソッドの組み合わせで実行したい処理を記述する。
- 返り値はそのアドレスに対して行われた処理の結果
- URIにアクセスした際の処理をControllerに移して扱うことでコードの整理をすることができ、保守性が高まる。

### @extends・@section・@yieldの関係性とbladeを分割するメリット
- @extendsはレイアウトの継承場所を設定できるものでベースのbladeファイルに名前を付けた@yieldを置くことで継承したBladeファイルの同じ名前の@sectionの中のコードを配置できる。
- ベースとなるBladeファイルを作って変更部分に@yieldを置くことでベースの骨組みを持ったページを複製し、子Bladeファイルに変更部分しか記載されないので可読性が向上する。

### @csrfは何のための記述か
- CSRF対策のため。トークンを作成して意図しないルートでのリクエストを拒否する。

### {{ }}とは何の省略系か
- Bladeで記載する<?php  ?>の省略形。中身はPHPの処理として認識される。
